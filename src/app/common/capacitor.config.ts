import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.econogruas.app',
  appName: 'econogruas',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
